# Lidera+

Mais liderança. Mais presença. Mais resultado.

- `docs/01-arquitetura-tecnica.md` — stack, decisões, estrutura de pastas, convenções.
- `docs/02-plano-mvp.md` — etapas do MVP e dados de acesso ao projeto Supabase.
- `supabase/schema.sql` — DDL completo (já aplicado ao projeto Supabase **lidera+**).
- `supabase/rls_policies.sql` — Row Level Security por perfil (já aplicado).
- `src/` — scaffold de pastas do Next.js seguindo as rotas do prompt master.

## Status

**MVP fechado (Etapas 0 a 8 do `docs/02-plano-mvp.md`).** Ver `docs/06-fechamento-mvp.md` para o resumo da Etapa 8 (bugs reais encontrados na revisão de permissões, checklist LGPD, logo/PWA, deploy). Itens fora do MVP original ficam listados no fim deste arquivo.

- **Etapas 0 a 7** do `docs/02-plano-mvp.md` estão implementadas: infraestrutura Supabase, autenticação + cadastro de usuários, CRUD de Lideranças e Apoiadores, Demandas + Atendimentos + "Pessoa Atendida", Dashboard, Relatórios (Módulo 11.1 e 11.5) com PDF e impressão, Aniversariantes + Alertas automáticos, e o Mapa Territorial (Módulo 8).
- **Etapa 8 (Fechamento do MVP):** logo oficial vetorizada (`public/brand/`) aplicada no login/Sidebar/favicon/ícones PWA; manifest + `next-pwa` configurados (`docs/06-fechamento-mvp.md` tem o detalhe); revisão de permissões ponta a ponta encontrou e corrigiu **dois bugs reais** (função RLS quebrada pra todo usuário `lideranca`, e o middleware bloqueando a própria logo na tela de login); checklist LGPD encontrou e corrigiu um gap real (`consent_whatsapp` nunca era checado antes de mostrar o botão de WhatsApp); documentação de deploy em `docs/05-deploy.md`.
- **Etapa 7 (Mapa Territorial):** tela `/mapa` com Leaflet + OpenStreetMap (`src/components/map/territory-map.tsx`, carregado via `next/dynamic({ ssr: false })` em `territory-map-loader.tsx` porque o Leaflet mexe em `window`/`document` direto). Mostra pins de lideranças e demandas com `latitude`/`longitude` preenchidos, coloridos pelo status de cada uma (`LEADER_STATUS_COLOR`/`DEMAND_STATUS_COLOR` de `types/domain.ts`, traduzidos pra hex em `lib/map-colors.ts`), com filtro por bairro e popup com link direto pro cadastro (`/liderancas/[id]` ou `/demandas/[id]`) — critério de pronto da Etapa 7. `leaflet`/`react-leaflet`/`@types/leaflet` já estavam no `package.json` desde o scaffold inicial.
- Sem geocodificação automática por endereço no MVP (nota já deixada em `supabase/schema.sql`): `latitude`/`longitude` são campos numéricos simples, preenchidos manualmente por enquanto. Registros sem essas colunas preenchidas simplesmente não aparecem no mapa (mensagem própria pra isso na tela, não erro).
- Nenhuma restrição de role nova no mapa: a RLS de `leaders`/`demands` já garante que uma liderança só vê a própria linha e as próprias demandas (mesmo padrão do resto do app) — não precisou filtrar nada extra no service.
- **Etapa 6 (Aniversariantes e Alertas):** tela `/aniversariantes` com filtro hoje/amanhã/semana/mês, WhatsApp com template semeado e botão "marcar como cumprimentado"; Edge Function `daily-alerts` (Módulo 10) rodando 1x/dia via `pg_cron` + `pg_net` (ver `docs/03-agendamento-edge-function.md`) que marca demandas vencidas como `atrasada`, notifica demandas/atendimentos vencendo hoje ou atrasados e agrega aniversariantes do dia em `notifications`; sino no Topbar (`src/components/layout/topbar.tsx`) com dropdown das últimas notificações, link para o registro relacionado, marcar 1/todas como lidas.
- Testei a Edge Function de ponta a ponta com um caso real (demanda com prazo vencido) e encontrei/corrigi um bug de contagem antes de considerar a Etapa 6 pronta — detalhes em `docs/03-agendamento-edge-function.md`. **Pendência conhecida:** o caminho de inserção de notificação em si (quando existe algum `admin_geral`/`admin_equipe` ativo) não foi exercitado ainda porque este projeto Supabase não tem nenhum usuário desse tipo cadastrado — validar isso é o primeiro teste a fazer assim que o primeiro Admin Geral for convidado.
- Sino de notificações também não rodou de verdade (mesma limitação de sempre, sem `npm install`/`npm run dev` no sandbox) — revisão manual de imports/exports feita, mas vale conferir visualmente o dropdown (posicionamento, clique fora fechando) na primeira rodada local.
- PDF gerado com `@react-pdf/renderer` via Route Handler Node.js (`runtime = "nodejs"` — essa lib não roda em Edge Runtime). **Isso não foi testado rodando de verdade** (mesma limitação de sempre: sem `npm install` no sandbox); é o ponto do projeto com maior risco de precisar de um ajuste fino ao rodar pela primeira vez — se o layout de tabela dentro do PDF vier estranho, o arquivo a mexer é `src/lib/pdf/shared-styles.ts` e os dois `*-report-document.tsx`.
- Relatórios de crescimento, ranking de lideranças e bairros fracos (Módulo 11.2 a 11.7) ficam para a v2 — não fazem sentido com pouco histórico de dados.
- Dashboard segue a ordem do padrão F4 Signal descrita em `docs/01-arquitetura-tecnica.md` §5: 5 cards executivos no topo, alertas (demandas atrasadas + aniversariantes do dia) logo abaixo — antes dos gráficos, porque é o que exige ação hoje — e o gráfico de apoiadores por bairro (Recharts) por último.
- "Demanda atrasada" no dashboard é calculada comparando `due_date` com a data de hoje, não lendo o campo `status = 'atrasada'` — porque nada ainda muda esse campo sozinho. Isso é resolvido de verdade na Etapa 6 (alertas automáticos via Edge Function agendada).
- Regra central do Módulo 5/6/7 aplicada de verdade: toda mudança de status de demanda ou atendimento passa por `updateDemandStatus`/`updateAttendanceStatus` (nunca um `.update()` solto numa página) — isso grava o histórico em `demand_updates`, marca `completed_at`/`attended_at` e registra a interação automaticamente (Módulo 14). A tela `/pessoas-atendidas/[id]` só lê esses dados via join; não existe tabela ou cópia separada de "pessoa atendida".
- Camada de serviço em `src/services/` (leaders, supporters, users) reaproveitada por Server Components e Server Actions — nenhuma query duplicada em página.
- Regras de negócio aplicadas no código, não só no banco: liderança só cadastra/edita a própria rede, não pode se auto-promover a "estratégica" nem mudar seu nível de influência; apoiador tem detector de duplicidade (telefone, nome+nascimento, nome+endereço) com opção de "cadastrar mesmo assim"; toda liderança/apoiador tem botão WhatsApp (`wa.me`) sem custo de API.
- Consentimento LGPD (Módulo 15) é campo obrigatório no cadastro de apoiador — sem marcar, o formulário não salva.
- **Não rodei `npm install`/`npm run build`** — o sandbox onde este código foi gerado bloqueia acesso ao registro do npm (erro 403 Forbidden). Rode `npm install && npm run dev` localmente antes de seguir. Fiz revisão manual de todos os imports/exports (todo arquivo que importa de `@/...` foi conferido contra o que o módulo de origem exporta).
- Falta menor: a página de detalhe da liderança ainda mostra só contagem de demandas/apoiadores, não a lista em aba — dá para reaproveitar o componente `Tabs` (`src/components/ui/tabs.tsx`) criado para "Pessoa Atendida" quando for a vez desse ajuste.
- Seletor de "pessoa relacionada" nos formulários de demanda/atendimento é um `<select>` simples com a lista inteira de apoiadores — funciona no MVP, mas vira uma combo gigante em bases grandes. Trocar por um combobox com busca �